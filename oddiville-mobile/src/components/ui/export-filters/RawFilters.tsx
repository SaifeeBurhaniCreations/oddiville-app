import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const RawFilters = () => {
  return (
    <View>
      <Text>RawFilters</Text>
    </View>
  )
}

export default RawFilters

const styles = StyleSheet.create({})

// import { StyleSheet, Text, View } from 'react-native'
// import React from 'react'

// const RawFilters = ({ state, setState }) => {
//   return (
//     <TimeRange state={state} setState={setState} />
//       <VendorSelector state={state} setState={setState} />
//       <MaterialSelector state={state} setState={setState} />
//       <StatusSelector state={state} setState={setState} />
//   )
// }

// export default RawFilters

// const styles = StyleSheet.create({})