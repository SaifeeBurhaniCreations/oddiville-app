import { StyleSheet, View } from "react-native";
import { Text } from "react-native-svg";

const PackagingScreen = () => {
  
  return (
    <View>
      <Text>PackagingScreen</Text>
    </View> 
  );
};

const styles = StyleSheet.create({});

export default PackagingScreen;
